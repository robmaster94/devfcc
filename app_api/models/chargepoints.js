'use strict'

/*const mongoose = require('mongoose')
const Schema = mongoose.Schema

const ChargerSchema = Schema({
  name: String,
  city: String,
  address: String,
  masterpass: { type: String, select:false }
})

module.exports = mongoose.model('Charger', ChargerSchema)


    Este modulo se habilitaria cuando se escalara la aplicacion para
    una mejor gestion de cargadores. Esta deshabilitado ya que se trabaja
    con una sola estacion de recarga (1 cargador) 


*/