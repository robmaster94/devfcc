<script type="text/javascript">
  const myHeaders = new Headers();
  myHeaders.append('authorization',`Bearer ${localStorage.token}`)

  fetch('/api/product', {
    method: 'GET',
    headers: myHeaders
  })
  .then(res => res.json())
  .then(data => {
      console.log(data)
  })

</script>


  <h2 class="bg-primary text-center"> Lista de elementos </h2>
  <ul class="list-group">
    <li class="list-item" ng-repeat="p in productos">{{p.id}}</li>
  </ul>
